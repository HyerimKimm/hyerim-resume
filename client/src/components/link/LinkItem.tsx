import React from 'react';
import { link } from '../../types/data';

type LinkItemPropsType = {
  link: link;
};

const LinkItem = ({ link }: LinkItemPropsType) => {
  return <div></div>;
};

export default LinkItem;
