import React, { useState } from 'react';
import styled from 'styled-components';

function App() {

  return (
    <AppContainer>
      안녕
    </AppContainer>
  );
}

const AppContainer = styled.div`
  width: 100vw;
  height: 100vh;
`

export default App;
